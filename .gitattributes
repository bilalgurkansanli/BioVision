# Normalise line endings in the repository. Development happens on Windows and CI
# runs on Linux; without this, every file round-trips as a whole-file diff.
* text=auto eol=lf

# Binary assets must never be munged.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.heic binary
*.pt binary
*.onnx binary
*.ipynb binary
