/**
 * Site-wide constants.
 *
 * Lives here rather than in `app/layout.tsx` because Next only permits a fixed
 * set of exports from a layout file — a stray constant there is a build error,
 * not a style question.
 */

export const SITE_URL = "https://biovision.bilalgurkansanli.com";
export const SITE_NAME = "BioVision";
